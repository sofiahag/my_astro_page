---
import { GoogleAnalytics } from '@astrolib/analytics';

import { SITE } from '~/config.mjs';
import { MetaSEO } from '~/types';
import { getAsset } from '~/utils/permalinks';

import CustomStyles from '~/components/CustomStyles.astro';

export interface Props extends MetaSEO {
  dontUseTitleTemplate?: boolean;
}
---
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<meta name="description" content="Hi! 👋 I'm Sofia Haglund, a Bioinformatican and System Developer.
I'm a creative, passionate, and independent individual. I love to code, particularly in JavaScript."/>
<meta name="keywords" content="bioinformatician, system developer, sofia haglund"/>

<CustomStyles />

<!-- Google Site Verification -->
{SITE.googleSiteVerificationId && <meta name="google-site-verification" content={SITE.googleSiteVerificationId} />}

<!-- Google Analytics -->
{SITE.googleAnalyticsId && <GoogleAnalytics id={String(SITE.googleAnalyticsId)} partytown={true} />}


<link rel="shortcut icon" href={getAsset('/profile-pic.png')} />
<link rel="mask-icon" href={getAsset('/IMG_0572_2.png')} color="#8D46E7" />
<link rel="sitemap" href={getAsset('/sitemap-index.xml')} />
